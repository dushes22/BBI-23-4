using System;

class Program
{
    static void Main()
    {
        double sum = 0;
        for (int i = 2; i <= 112; i += 2)
        {
            sum += (double)i / (i + 1);
        }

        Console.WriteLine("Сумма ряда: " + sum);
    }
}
